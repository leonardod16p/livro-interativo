Coloque aqui um arquivo chamado "ambiente.mp3" (música/som ambiente em loop)
para tocar automaticamente quando o jogo começar.

Se o arquivo não existir, o jogo funciona normalmente (só sem música de fundo) —
os efeitos de som das missões são gerados via Web Audio API e não dependem de arquivos.
